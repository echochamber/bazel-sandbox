version: v1
name: buf.build/jschein/grpc-gateway

breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
deps:
  - buf.build/jschein/rdproto
  - buf.build/grpc-ecosystem/grpc-gateway
  # So that export includes WKTs that are imported by other protos.
  # - buf.build/protocolbuffers/wellknowntypes